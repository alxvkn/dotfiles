# i3blocks configuration file
#
# The i3blocks man page describes the usage of the binary,
# and its website describes the configuration:
#
#     https://vivien.github.io/i3blocks


# Global properties
separator=true
separator_block_width=15

[sway/window]
command=$SCRIPT_DIR/sway/window
interval=persist

[playerctl]
command=$SCRIPT_DIR/playerctl
interval=persist
markup=pango

[memfree]
command=free -h | awk '/Mem/{print "󰍛 " $3 "/" $2 }'
interval=2

[adb#tablet]
DEVICE_PATTERN=Lenovo
LABEL=󰓶
command=$SCRIPT_DIR/adb
interval=persist
markup=pango

[adb#phone]
DEVICE_PATTERN=SM
LABEL=󰄜
command=$SCRIPT_DIR/adb
interval=persist
markup=pango

[battery]
command=$SCRIPT_DIR/battery
interval=5
markup=pango

[diskspace#root]
LABEL=
MOUNTPOINT=/
command=$SCRIPT_DIR/diskspace
interval=persist

[pulseaudio]
command=$SCRIPT_DIR/pulseaudio
interval=persist
min_width=36

[sway/keyboard_layout]
command=$SCRIPT_DIR/sway/keyboard_layout
interval=persist

[date]
command=[ "$button" ] && notify-send -t 20000 -c calendar -a Calendar "$(cal)"; date '+%a %d.%m.%Y %X'
# command=date '+%A %x %X'
interval=1

# vim: ft=dosini
