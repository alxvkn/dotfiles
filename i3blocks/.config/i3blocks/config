# i3blocks configuration file
#
# The i3blocks man page describes the usage of the binary,
# and its website describes the configuration:
#
#     https://vivien.github.io/i3blocks


# Global properties
separator=true
separator_block_width=15

[sway/window]
command=$SCRIPT_DIR/sway/window
interval=persist

[playerctl]
command=$SCRIPT_DIR/playerctl
interval=persist
markup=pango

[adb#tablet]
DEVICE_PATTERN=Lenovo
LABEL=󰓶
command=$SCRIPT_DIR/adb
interval=persist
markup=pango

[adb#phone]
DEVICE_PATTERN=SM
LABEL=󰄜
command=$SCRIPT_DIR/adb
interval=persist
markup=pango

[battery]
command=$SCRIPT_DIR/battery
interval=5
markup=pango

[stats]
SEP= | 
LABEL=󰍛
MEM_LABEL=󰍛
STORAGE_LABEL=󰋊
command=$SCRIPT_DIR/stats
interval=persist

[pulseaudio]
command=$SCRIPT_DIR/pulseaudio
interval=persist

[gnome_dark_mode]
command=$SCRIPT_DIR/gnome_dark_mode
min_width=
interval=persist

[sway/keyboard_layout]
command=$SCRIPT_DIR/sway/keyboard_layout
interval=persist

[date]
command=[ "$button" ] && notify-send -t 20000 -c calendar -a Date "$(date '+%A %d.%m.%Y')" "$(cal)"; date '+%X'
# command=date '+%A %x %X'
interval=1

# vim: ft=dosini
